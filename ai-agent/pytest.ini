[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --cov=. --cov-report=term-missing --cov-report=html --cov-config=.coveragerc
markers =
    integration: 실제 인프라(Kafka 컨테이너, PostgreSQL)를 띄우는 통합 테스트. Docker 없으면 skip.
